name: CI

on:
  pull_request:
    branches: [main]

  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-test:
    name: ${{ matrix.os }}
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-24.04
          - macos-15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Conan
        run: |
          python -m pip install --upgrade pip
          pip install conan

      - name: Cache Conan
        uses: actions/cache@v4
        with:
          path: ~/.conan2
          key: conan-${{ runner.os }}-${{ hashFiles('conanfile.py') }}
          restore-keys: |
            conan-${{ runner.os }}-

      - name: Install clang-tidy (macOS)
        if: runner.os == 'macOS'
        run: |
          brew install llvm

      - name: Detect Conan Profile
        run: |
          conan profile detect --force

      - name: Install Dependencies
        run: |
          conan install . \
            --build=missing \
            -s build_type=Release

      - name: Configure
        run: |
          cmake --preset release

      - name: Build
        run: |
          cmake --build build/release

      # - name: Test
      #   run: |
      #     ctest \
      #       --test-dir build/release \
      #       --output-on-failure

      - name: Clang-Tidy
        if: runner.os == 'Linux'
        shell: bash
        run: |
          find src tests \
            \( -name "*.cpp" -o -name "*.cc" -o -name "*.cxx" \) \
            -print0 | \
          xargs -0 -n1 clang-tidy -p build/release

     
      - name: Run clang-tidy
        if: runner.os == 'macOS'
        run: |
          LLVM_PREFIX=$(brew --prefix llvm)

          find src tests \
            \( -name "*.cpp" -o -name "*.cc" -o -name "*.cxx" \) \
            -print0 | \
          xargs -0 -n1 $LLVM_PREFIX/bin/clang-tidy -p build/release
      
        